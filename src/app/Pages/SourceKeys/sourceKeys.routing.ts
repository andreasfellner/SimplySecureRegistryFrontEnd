import { Routes } from '@angular/router';
import { SourceKeyComponent } from './SourceKeys/sourceKeys.component';

export const SourceKeyRoutes: Routes = [
    {
        path: '',
        component: SourceKeyComponent
    }
]
